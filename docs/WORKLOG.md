# Worklog

## Now / Next
<!-- Checklist. "Now" items first (1-3 max), then queued items. -->
- [ ]

### Ideas
<!-- Unscoped, someday. -->

## Session notes
<!-- Newest entry at top. Format: YYYY-MM-DD | Shipped: ... | Next: ... | Blockers: ... -->
2026-07-09 | Shipped: Folded confirmed research on how Claude Code loads context into all three skills. drift-check gained: (1) a line distinguishing CLAUDE.md from Claude Code's separate auto memory system (`~/.claude/projects/<project>/memory/MEMORY.md`), explicitly out of scope; (2) Check 3 (compaction) rewritten with the confirmed mechanism, project-root CLAUDE.md/CLAUDE.local.md auto-reinject after `/compact` so a content ❌ there is a real finding, nested-directory CLAUDE.md files don't auto-reinject so a ❌ there can still be expected fallout, and behavioral (Check 2) ❌s are always expected fallout regardless of file scope; (3) the auto-remediation "file may be too long" guardrail now cites Anthropic's own under-200-lines-per-file guidance instead of an assumed threshold. project-init gained a one-line pointer to `CLAUDE.local.md` (official, gitignored personal-preferences companion to the shared CLAUDE.md it creates) and added `CLAUDE.local.md` to the default `.gitignore` template so the pointer and the scaffolding stay consistent. README's intro corrected after checking Claude's account-level Settings > Customize > Skills panel: skills are NOT Claude Code-only, they install there via account-level upload (zip + "Upload a skill") and work identically across Claude Code, Claude.ai chat, and Cowork; the earlier draft of this session's README change wrongly claimed skills don't work in Chat/Cowork at all, corrected before commit. Added a "Where these three skills are actually useful" section instead, scoped to what actually gates *these specific skills* (file access and CLAUDE.md auto-load, not skill-install-path): Claude.ai chat has no file access so none of the three can do anything there regardless of install method; Cowork has file access via a connected folder so project-init and handoff could genuinely work there; drift-check specifically still won't, its mechanism depends on CLAUDE.md auto-loading every session the way Claude Code does, which Cowork doesn't do automatically. All changes sourced from Anthropic's official docs (code.claude.com/docs/en/memory, support.claude.com skills articles) and Claude Cowork's own self-reported behavior when asked directly, not assumptions. Diffed all three skills against the private originals in `~/my-ai-agent`; ported six handoff improvements that were missing here: Step 0 early exit for no-op sessions; same-day worklog entries stay separate by default; Shipped as semicolon-separated clauses not prose; no time-of-day sub-narration; ~500-char soft length guardrail; decision-log archiving at 15 entries to `docs/archive/decisions-YYYY.md`. Ported one drift-check improvement: compact output format (single summary line per check when clean, full per-rule expansion only on failure), replacing the always-expanded checklist. project-init confirmed already in sync, no gaps. | Next: none pending from this session.

2026-07-03 | Shipped: published project-init, handoff, and drift-check as public skills at github.com/ngreenwall/claude-skills, each validated live in a scratch dir; scoped drift-check to the global context file only (added Setup section with 3 labeled canary tokens + drift-check/example-global-CLAUDE.md template); fixed a contradictory AGENTS.md guardrail between project-init and handoff; iterated drift-check's remediation wrap-up twice (tagged line, then rewritten to plain-language sentences per fix type, incl. confirming a prior check's fix); added "starting a new session"/"before I start fresh" as handoff trigger phrases; rewrote README with per-skill overviews and drift-check setup walkthrough; ported these same decisions to the private skills in ~/my-ai-agent; clarified in the README that SKILL.md auto-invocation is Claude Code only, added a "Using these in Cursor" conversion path (.mdc project rule, @-invocation), and noted Cursor User Rules as drift-check's alternative global-file target; added a Contents TOC, linked each skill heading to its SKILL.md, and added a License section pointing at the MIT LICENSE file; set the GitHub repo description and topics (`claude-code`, `claude`, `cursor`, `ai-agents`, `developer-tools`) via `gh repo edit`, since none were set and the repo was invisible in GitHub search/browse; fixed two broken markdown links in README (canary-token example and LICENSE link had gotten wrapped in backticks); linked the drift-check example earlier in its own setup step 1; added a note to drift-check/SKILL.md's Setup pointing users to replace the Check 2 "Core rules" placeholders with their own always-on rules; clarified that setup step 5 (noting the file path) only applies to non-standard file locations, and flagged in the Cursor section that step 3/5 (file path) doesn't apply since Cursor User Rules aren't a plain file with a path; normalized README's extra blank lines to single spacing.
<!-- Archive to docs/archive/worklog-YYYY-MM.md when list exceeds 10 entries -->
