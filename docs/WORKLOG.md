# Worklog

## Now / Next
<!-- Checklist. "Now" items first (1-3 max), then queued items. -->
- [ ]

### Ideas
<!-- Unscoped, someday. -->

## Session notes
<!-- Newest entry at top. Format: YYYY-MM-DD | Shipped: ... | Next: ... | Blockers: ... -->
2026-07-09 | Shipped: added `qa-audit/SKILL.md` as the fourth public skill (rewritten from `~/my-ai-agent/.rulesync/skills/qa-audit`, stripped of rulesync-specific mechanics), validated live in a scratch dir, updated README.md and CLAUDE.md; then ran qa-audit itself (3 parallel Opus subagents) against `handoff/SKILL.md`, `project-init/SKILL.md`, `drift-check/SKILL.md` and fixed 12 findings: handoff (Step 9's literal `$WORKLOG_PATH` written to CLAUDE.md fixed to hardcoded `docs/WORKLOG.md`, added a CLAUDE.md-creation fallback, expanded the commit-checklist enum, aligned archive-threshold wording, fixed a stale section-name reference); project-init (dropped a "prepare handoff files" trigger phrase that collided with handoff, fixed LICENSE-creation wording to match actual git-gated behavior, added `CLAUDE.local.md` to the simple branch's `.gitignore`, added a `gh`-unavailable fallback); drift-check (named Claude Code explicitly instead of generic "AI tool" phrasing, de-duplicated a re-read framing in auto-remediation, made canary lookup consistent across top/mid/bottom); also reworded a now-stale "three specific skills" phrase in CLAUDE.md's per-surface gating decision to drop the count. | Next: none pending from this session.

2026-07-09 | Shipped: ran an Opus QA pass over `drift-check/SKILL.md`, `handoff/SKILL.md`, `project-init/SKILL.md`, cross-checked against README and decision log; no behavior-breaking bugs found (prior override-read fix confirmed good); fixed 4 style findings: replaced 4 em dashes in `handoff/SKILL.md` (Step 6 spec/decision headers, Step 7 delete-not-check-off line, Step 12 commit line) with colons/parens/commas per repo's own no-em-dash rule; drift-check's canary example changed from `anchor-word-word-NN` to neutral `word-word-NN`, `anchor-` was an unintentional echo of the author's real personal token prefix; drift-check's Check 2 override sub-check now clarifies the project CLAUDE.md is read only to detect explicit overrides, not audited for its own drift, resolving apparent tension with the "out of scope" scope statement; dropped a stale "$WORKLOG_PATH substitution" instruction in handoff Step 9 whose template was already hardcoded to `docs/WORKLOG.md`, matching project-init's equivalent template. | Next: none pending from this session.

2026-07-09 | Shipped: user-reported line numbers (55, 36) for two `drift-check/SKILL.md` wording issues didn't match the file, review found no em dash anywhere in the file; located the actual issues by content instead: split a 3-clause comma-splice in Check 2 ("If Check 1 fails, distrust a clean Check 2, the model likely cannot grade itself reliably either.") into two sentences; the Check 2 override sub-check said only "check whether a lower-precedence file... explicitly marks itself as overriding," without ever instructing a read of that project file, now explicitly says to read the project's `CLAUDE.md` alongside the already re-read global file, since detecting the override requires both. | Next: none pending from this session.

2026-07-09 | Shipped: resolved both open Now/Next items using the decision reached in the parallel personal-skill session: project-vs-global CLAUDE.md conflicts resolve as explicit-override-only (project wins only with an explicit marker; unmarked conflicts stay drift, global stays authoritative), rejecting "project always wins"/"global always wins"; applied to `context-router.md`'s canonical template (live file, `handoff`, both `project-init` variants) and to `drift-check`'s Check 2; logged as a `CLAUDE.md` decision; not yet tested against a real project-vs-global conflict in this repo. | Next: none pending from this session.

2026-07-09 | Shipped: QA'd `drift-check/SKILL.md`: fixed Check 3, which described project-root/subdirectory CLAUDE.md reinjection despite the skill's own global-file-only scope, verified against Anthropic's memory docs, rewrote to describe the global file directly; added a missing `Canary (top):` label example in Setup; Check 2 now handles first-reply-of-session as `n/a` instead of leaving it undefined; added a `Drift check (simulated):` label for forced/test runs so they're never mistaken for real drift; removed the Cursor-adaptation paragraph from Setup entirely, drift-check is Claude Code only now, matching what the README already claimed; superseded and archived the 2026-07-03 Cursor-template decision in `CLAUDE.md`, replaced by a 2026-07-09 entry recording the Claude-Code-only scope. | Next: Check 2 still needs a precedence-override rule (an apparent ❌ that's actually a deliberate, correct override by a lower-precedence file shouldn't grade as drift); separately, `context-router.md`'s canonical template doesn't say which wins when a project `CLAUDE.md` and the global context file directly conflict, worth a decision (affects `handoff` and `project-init` too, not just drift-check).

2026-07-09 | Shipped: `handoff/SKILL.md` Step 0 early-exit rule now judges by session content first, files edited/created or commits made this session, including mid-conversation commits made outside the handoff flow, `git status --porcelain` is only a confirmation check once session content already says nothing happened, not the primary signal, since a clean tree can just mean earlier work was already committed and pushed outside handoff, not that nothing shipped; tightened the wording of that same paragraph for readability, no behavior change. | Next: none pending from this session.

2026-07-09 | Shipped: `handoff/SKILL.md` now instructs superseding a decision entry and writing a fresh one, instead of stacking a second inline "(Correction, ...)" onto it; applied that to `CLAUDE.md`'s 2026-07-03 auto-invocation entry, superseded and archived to `docs/archive/decisions-2026.md`, replaced by a single current-state 2026-07-09 entry (Cursor has no skills mechanism; Claude.ai/Cowork upload path is Customize > Skills > "+" > "+ Create skill" > "Upload a skill"). | Next: none pending from this session.

2026-07-09 | Shipped: README audited against current Anthropic/Cursor docs, several inaccuracies fixed: skill-upload path corrected to match support.claude.com (Customize > Skills > "+" > "+ Create skill" > "Upload a skill"); Claude.ai chat's file-access claim softened to no persistent project-folder access; Skill Overview reworded to note skills can bundle scripts; broken TOC anchor fixed for the install section; two markdown links unwrapped from backticks (example-global-CLAUDE.md, LICENSE); drift-check tagged Claude Code only in TOC/heading; redundant prose trimmed across sections; one-line quote summaries added under each skill heading; Skill Overview heading removed, its paragraph folded into the intro; user reformatted headings (removed `---` dividers, `24e0a42`); the `example-global-CLAUDE.md` link reverted to backtick-wrapped (non-clickable) during that edit and was fixed again, `3648aa8`. | Next: none pending from this session.

2026-07-09 | Shipped: README corrected on skill availability (see CLAUDE.md decision log). drift-check: auto-memory scoping note; Check 3 compaction logic split by file scope; <200-line guidance cited; compact clean/failure output format. handoff: no-op early exit; same-day-entries rule; semicolon-clause Shipped style; no time-of-day narration; ~500-char guardrail; decision-log archiving at 15. project-init: `CLAUDE.local.md` pointer + `.gitignore` entry. Commit 44fa70a. | Next: none pending from this session.

2026-07-03 | Shipped: published project-init, handoff, and drift-check as public skills at github.com/ngreenwall/claude-skills, each validated live in a scratch dir; scoped drift-check to the global context file only (added Setup section with 3 labeled canary tokens + drift-check/example-global-CLAUDE.md template); fixed a contradictory AGENTS.md guardrail between project-init and handoff; iterated drift-check's remediation wrap-up twice (tagged line, then rewritten to plain-language sentences per fix type, incl. confirming a prior check's fix); added "starting a new session"/"before I start fresh" as handoff trigger phrases; rewrote README with per-skill overviews and drift-check setup walkthrough; ported these same decisions to the private skills in ~/my-ai-agent; clarified in the README that SKILL.md auto-invocation is Claude Code only, added a "Using these in Cursor" conversion path (.mdc project rule, @-invocation), and noted Cursor User Rules as drift-check's alternative global-file target; added a Contents TOC, linked each skill heading to its SKILL.md, and added a License section pointing at the MIT LICENSE file; set the GitHub repo description and topics (`claude-code`, `claude`, `cursor`, `ai-agents`, `developer-tools`) via `gh repo edit`, since none were set and the repo was invisible in GitHub search/browse; fixed two broken markdown links in README (canary-token example and LICENSE link had gotten wrapped in backticks); linked the drift-check example earlier in its own setup step 1; added a note to drift-check/SKILL.md's Setup pointing users to replace the Check 2 "Core rules" placeholders with their own always-on rules; clarified that setup step 5 (noting the file path) only applies to non-standard file locations, and flagged in the Cursor section that step 3/5 (file path) doesn't apply since Cursor User Rules aren't a plain file with a path; normalized README's extra blank lines to single spacing.
<!-- Archive to docs/archive/worklog-YYYY-MM.md when list exceeds 10 entries -->
